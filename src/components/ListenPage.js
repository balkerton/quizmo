import React from 'react';

const ListenPage = () => {
		return(
				<h1 className="header">This will be the page where the audio is recorded and the answers logged.</h1>
			)
}

export default ListenPage;